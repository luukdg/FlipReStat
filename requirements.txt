cloudscraper==1.2.71
discord.py==2.5.0
fake_useragent==2.0.3
Brotli==1.1.0
aiohttp==3.8.5  # Older version due to compatability issues with the server
aiohappyeyeballs==2.4.0  # Older version due to compatability issues with the server